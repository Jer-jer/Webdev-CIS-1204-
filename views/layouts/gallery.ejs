<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<link rel="shortcut icon" href="images/favi2.png">
	<meta name="viewport" content="width=device-width", initial-scale="1.0">
    <meta http="X-UA-Compatible" content="ie=edge">
    <link href="css/gallery.css" rel="stylesheet">
	<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link rel="stylesheet" href="css/eacss/navbar.css" type="text/css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="js/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/2.0.2/anime.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <title>Gallery</title>
</head>
<body>
<%- include('../partials/navbar.ejs', {active: "Gallery"}) %>
<%- include('../gallery/index.ejs') %>
</body>
<script type=text/javascript>
 text = document.getElementById("text");

	function split(element){
	  const content = element.innerText
	  const length = content.length
	  element.innerText = ""
	  
	  for(let i = 0; i<length;i++){
	     const span = document.createElement("span")
	     span.innerText = content[i]
	     span.id = "split_"+i
	     span.className = "split_span"
	     span.style.opacity = 0
	     element.append(span)
	  }
	}

	function animate(element, animation){
	 split(element)

	 let indexes = []
	 const letters =  document.querySelectorAll("#"+element.id + " .split_span")
	 for(let i = 0;i<letters.length;i++){
	   indexes.push(i)
	 }
	 indexes = Shuffle(indexes)
	 setInterval(function(){
	   if(indexes.length > 0){
	     let letter = letters[indexes[0]]
	     letter.style.animation = `${animation} 2s ease-in-out`
	     letter.style.opacity = 1
	     indexes.splice(0,1)
	   }
	 },100)
	}

	function Shuffle(o) {
		for(var j, x, i = o.length; i; j = parseInt(Math.random() * i), x = o[--i], o[i] = o[j], o[j] = x);
		return o;
	};

	animate(text, "fadeIn")
   </script>
    
    <script src="js/modernizr.custom.js"></script>
    <script src="js/masonry.pkgd.min.js"></script>
	<script src="js/imagesloaded.js"></script>
	<script src="js/classie.js"></script>
	<script src="js/AnimOnScroll.js"></script>
		<script>
		new AnimOnScroll( document.getElementById( 'grid' ), {
			minDuration : 0.4,
			maxDuration : 0.6,
			viewportFactor : 0.2
		} );
	</script>
	<script>
    document.getElementById("foo").onchange = function() {
        if (this.selectedIndex!==0) {
            window.location.href = this.value;
        }        
    };
</script>
</html>